<div
  class={
    `
    -top-1/2 -left-1/2 -bottom-1/2 -right-1/2
    fixed bg-repeat bg-transparent w-[200%] h-[200vh] z-[9999]
    pointer-events-none contrast-200 brightness-200
    `
  }
  data-noise
></div>

<style>

  div {
    background-image: var(--background-noise);
    mix-blend-mode: multiply;

    animation: noise 1s steps(10) infinite;
  }

  @media (prefers-reduced-motion: reduce) {
    div {
      animation: none;
    }
  }

  @keyframes noise {
    0% {
      transform: translate(0, 0);
    }
    10% {
      transform: translate(-5%, -5%);
    }
    20% {
      transform: translate(-10%, 5%);
    }
    30% {
      transform: translate(5%, -10%);
    }
    40% {
      transform: translate(-5%, 15%);
    }
    50% {
      transform: translate(-10%, 5%);
    }
    60% {
      transform: translate(15%, 0);
    }
    70% {
      transform: translate(0, 15%);
    }
    80% {
      transform: translate(10%, 10%);
    }
    90% {
      transform: translate(5%, 5%);
    }
    100% {
      transform: translate(0, 0);
    }
  }

</style>